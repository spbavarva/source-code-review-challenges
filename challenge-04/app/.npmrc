# npm configuration for metrics-collector
registry=https://registry.npmjs.org/

# Auth token for publishing/reading from the public registry.
//registry.npmjs.org/:_authToken=${NPM_TOKEN}

# Be quiet during CI installs.
fund=false
audit=false
